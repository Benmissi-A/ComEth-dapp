import {Heading, Link, Button} from "@chakra-ui/react"

const VoteTemplate = () => {
  return (<> 
  <Heading>Just Vote</Heading>
  <Link to="/home">
        <Button>Home</Button>
      </Link>
  </>) 
}
export default VoteTemplate